
Follow the steps below to run the experiment. Run
all commands from cs244_final/ directory unless mentioned otherwise.

1. Run INSTALL.sh for one time setup only.

2. Run the script ./run_openflow.sh

3. Once you see 'Ready' in step 2, in another console, run the following:
sudo ./run_experiment.sh

4. The script dumps data in 'results' directory for each of the runs.
There are 3 plots (*.png files) for different durations: 500ms, 5sec and 20 or 60 sec (depending on the experiment).